# Legacy → V10 Migration Map

- Legacy `.engineer` remains available as reference.
- Standalone startup moves to `ENGINEER_OS_V10/ENTRYPOINT.yaml`.
- Existing governance insights can be migrated as modules under `01_GOVERNANCE/modules/`.
- Existing workshop canons can be mirrored or linked into `05_WORKSHOP/`.
- Existing active project truth can be mounted under `06_ACTIVE_PROJECT/` via project-specific bootstrap.
