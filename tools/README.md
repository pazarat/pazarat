# Tools

This folder contains lightweight repository validation helpers.

- `verify_tunnel_integrity.py` checks root entry files, structural transformation gate wiring, Pazarat codebase-routing gate wiring, and common forbidden buried-tunnel paths.

Run from the repository root:

```bash
python tools/verify_tunnel_integrity.py
```
