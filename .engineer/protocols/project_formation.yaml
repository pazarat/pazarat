id: project_formation
purpose: "When ACTIVE_PROJECT is empty, gather initial facts and create a baseline without treating emptiness as failure."
steps:
  - route_intent
  - select_scope
  - touch_relevant_sources
  - apply_governance
  - produce_result
  - verify_against_intent
outputs:
  - "decision"
  - "evidence"
  - "next_action"
