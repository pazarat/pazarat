id: semantic_routing
purpose: "Classify the user request before scanning. Select route, scope, and tools."
steps:
  - route_intent
  - select_scope
  - touch_relevant_sources
  - apply_governance
  - produce_result
  - verify_against_intent
outputs:
  - "decision"
  - "evidence"
  - "next_action"
