id: runtime_self_audit
purpose: "Inspect the engineering runtime only when requested or when a runtime failure is detected."
steps:
  - route_intent
  - select_scope
  - touch_relevant_sources
  - apply_governance
  - produce_result
  - verify_against_intent
outputs:
  - "decision"
  - "evidence"
  - "next_action"
