# v9.7 Open Neural Kernel Changelog

- Restores v9-style authentic model behavior: no first-contact cage, no scripted answer renderer.
- Keeps runtime silent and project-centered.
- Adds `open-neural-entry` as a governed operating packet, not a final answer.
- Adds `truth-sweep-360` for content-value truth grounding.
- Adds `operation-360` for root-cause lifecycle handling.
- Adds `code-fingerprint`, `code-impact-360`, and `patch-gate-360` to reduce island-code and patchwork risk.
- Updates `plug-fast` to return open navigation choices instead of forcing factory/goal closure for every surface answer.
