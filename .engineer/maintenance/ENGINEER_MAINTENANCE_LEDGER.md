# Engineer Maintenance Ledger

This is a silent runtime-maintenance memory for future engineer sessions. It is not project truth and must not override current files.

## v7.0.0 — Content Value Atlas and Response Rhythm

- Added `value-atlas` as a project-neutral, file-type-neutral atlas before high-impact claims.
- Extended the neural matrix so content value is not limited to PRD files; code, tests, standards, decisions, documents, and configuration are cognitive evidence units.
- Added response rhythm governance: clear execution requests should be executed with a concise result, not answered with long theory.
- Added mandatory best-fit fit-check discipline inside the 360 method.
- Kept runtime neutral: no active-project-specific terms may be hardcoded in `.engineer` logic.

## Maintenance Rules

- Repository/runtime repairs must be applied to the versioned package when the request exceeds a trivial one-file text snippet.
- Future engineers should read this ledger only as maintenance memory; current repository files remain the source of truth.
- Any runtime change must run `runtime-audit`, `doctor`, and relevant version tests before packaging.

## v7.0 Indexed Kernel Decision

Problem: v6.19 made content awareness more general but added live-scan weight and weakened response rhythm.
Decision: move panoramic content touch into build-index and make plug fast by default, with plug-deep for selected high-risk operations.
Do not regress to adding broad scan layers into every request. Any future improvement must respect build-time vs run-time separation.

## 2026-05-26 — v9.0 Engineer Neural OS / Factory Core

- Added a stable factory architecture that binds intent, truth access, workshop lens, File Matrix, goal closure, strategy simulation, execution governance, validation, and ledger into one neural operating lifecycle.
- Added truth-grounded goal closure as mandatory governance, not an optional tool.
- Added capability registry so future layers are registered into slots rather than becoming isolated tools.
- Added CLI entries: `neural-os`, `factory-cycle`, `goal-closure`, `completion-audit`, `factory-health`.
- Updated central entry and navigation so high-impact work starts through the factory loop and cannot present partial work as complete without readiness verdict.

## 2026-05-26 — v9.1 Truth Pulse Integration

- Upgraded v9.0 baseline to v9.1 without reverting to v8.x branch.
- Added `file-matrix-pulse` as mandatory content-aware 360 truth touch.
- Integrated pulse into `plug-fast`, `goal-closure`, `factory-cycle`, tool registry, factory health, and truth-grounded goal governance.
- Fixed generated runtime cache staleness by excluding `.engineer/indexes`, `.engineer/state`, `.engineer/ledgers`, and `.engineer/backups` from runtime/repository File Matrix fingerprints.
- Preserved workshop boundary: `02_WORKSHOP/_WORKSHOP_SYSTEM` contains universal project standards; `02_WORKSHOP/ACTIVE_PROJECT` remains the active project truth surface inside the workshop.
