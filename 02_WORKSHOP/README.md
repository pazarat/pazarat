# Workshop

This is the project workspace.

Only `ACTIVE_PROJECT/` contains the real active project.

Everything under `_WORKSHOP_SYSTEM/` is support material: formation rules, templates, strategies, and checks. It is not the project.
